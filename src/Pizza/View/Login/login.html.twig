{% extends "baselayout.html.twig" %}

{% block content %}
    <h1>{{ "login"|trans({}, "frontend") }}</h1>
    <form action="{{ path('login_check') }}" method="post">
        {{ error }}
        <div class="fieldwrapper">
            <label for="_username">{{ "username"|trans({}, "frontend") }}</label>
            <input id="_username" type="text" name="_username" value="{{ last_username }}" />
        </div>
        <div class="fieldwrapper">
            <label for="_password">{{ "password"|trans({}, "frontend") }}</label>
            <input id="_password" type="password" name="_password" value="" />
        </div>
        <input class="btn btn-success" type="submit" />
    </form>
{% endblock content %}