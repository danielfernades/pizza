{% extends "baselayout.html.twig" %}

{% block content %}
    <h1>{{ "users"|trans({}, "frontend") }}</h1>
    {% if users|length > 0 %}
        <table class="table table-striped">
            <thead>
            <tr>
                <th>{{ "title"|trans({}, "frontend") }}</th>
                <th>{{ "actions"|trans({}, "frontend") }}</th>
            </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user }}</td>
                    <td>
                        <a class="btn" href="{{ url('user_edit', {'id': user.id }) }}">{{ "edit"|trans({}, "frontend") }}</a>
                        <a class="btn btn-danger" href="{{ url('user_delete', {'id': user.id }) }}">{{ "delete"|trans({}, "frontend") }}</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% endif %}
    <a class="btn" href="{{ url('user_edit') }}">{{ "create"|trans({}, "frontend") }}</a>
{% endblock %}